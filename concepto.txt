las mesas tienen 4 plazas
hay 4 franjas horarias
cada restaurante registra el nº de mesas que tiene
el restaurante puede cambiar el estado de la reserva

el cliente puede cancelar reserva
selecciona el numero de comensales



cliente->nombre usuario, contraseña, telefono
restaurante->nombre usuario, contraseña, telefono, nombre restaurante, capacidad, web, direccion, descripcion
reserva->id_reserva, id_cliente,id_restaurante, comensales, fecha, hora, estado(pendiente, confirmada, cancelada)

# Hashear una contraseña
def hash_password(password):
    salt = bcrypt.gensalt()
    hashed = bcrypt.hashpw(password.encode('utf-8'), salt)
    return hashed

# Verificar una contraseña
def check_password(password, hashed_password):
    return bcrypt.checkpw(password.encode('utf-8'), hashed_password)